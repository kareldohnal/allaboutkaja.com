import React from 'react';
import PropTypes from 'prop-types';
import NavItem from "./NavItem";

const Nav = props => {
  return (
    <nav>
      <NavItem />
    </nav>
  );
};

Nav.propTypes = {

};

export default Nav;
